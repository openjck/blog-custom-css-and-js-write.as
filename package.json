{
  "type": "module",
  "scripts": {
    "build": "esbuild --minify --outdir=build custom.css custom.js",
    "test": "npm run lint",
    "lint": "npm run format:check",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "esbuild": "^0.27.3"
  },
  "devDependencies": {
    "@openjck/prettier-config": "^1.0.2",
    "prettier": "^3.7.4"
  }
}
